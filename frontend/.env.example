# FuelBook frontend env template
# Copy to .env.local for local dev (and set in Vercel for production).
#
# Local dev (backend on your machine):
#   NEXT_PUBLIC_API_URL=http://localhost:4000
#
# Production (live backend on the Azure server):
#   NEXT_PUBLIC_API_URL=https://40-81-242-220.nip.io

NEXT_PUBLIC_API_URL=http://localhost:4000
